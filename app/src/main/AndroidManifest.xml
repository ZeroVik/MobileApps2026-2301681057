<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".MovieParadisoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="MovieParadiso"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieParadiso">

        <activity
            android:name=".ui.StatsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.FavoritesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.PlayerActivity"
            android:exported="false" />

        <activity
            android:name=".ui.MovieDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AddEditMovieActivity"
            android:exported="false" />

        <activity
            android:name=".ui.VlcPlayerActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            />

        <activity
            android:name=".ui.QrCodeActivity"
            android:exported="false" />



        <activity
            android:name=".ui.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>